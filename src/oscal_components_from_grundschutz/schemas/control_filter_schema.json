{
    "description": "Schema for the output of the control filtering model.",
    "type": "object",
    "properties": {
      "approved_controls": {
        "type": "array",
        "description": "A list of control IDs that have been approved for inclusion based on the criteria.",
        "items": {
          "type": "string",
          "description": "A control ID, e.g., 'SYS.1.1.A1'."
        }
      }
    },
    "required": ["approved_controls"]
  }